<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group
        android:orderInCategory="0"
        android:id="@+id/users_sFolder"
        android:checkableBehavior="single">
        <item
            android:id="@+id/nav_today_task"
            android:icon="@mipmap/ic_date_range_black_24dp"
            android:title="今天"
            android:checked="true"/>
        <item
            android:id="@+id/nav_collect_box"
            android:icon="@mipmap/ic_folder_black_24dp"
            android:title="收集箱" />
    </group>

    <!--该group用于存放动态添加的item-->
    <group
        android:orderInCategory="1"
        android:checkableBehavior="single"
        android:id="@+id/add_sFolder">

    </group>

    <group android:id="@+id/edit_sFolder"
        android:orderInCategory="2"
        android:checkableBehavior="single">
        <item
            android:id="@+id/nav_add_sfolder_list"
            android:icon="@mipmap/add_sfolder_list"
            android:title="添加清单" />
        <item
            android:id="@+id/nav_edit_sfolder_list"
            android:icon="@mipmap/edit_sfolder_list"
            android:title="编辑清单" />
    </group>

</menu>
